# ZFS on Linux Buildbot Patches

This directory contains patches which have been applied to the ZFS on
Linux buildbot master.  Each patch is fully described in its commit comment.
```
0001-fix-wait-for-request.patch
```
The patches cleanly apply on top of buildbot v3.0.2, but are not needed on
buildbot v3.2.x or later.
